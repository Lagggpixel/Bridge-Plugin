package me.lagggpixel.bridge.Commands;

import me.lagggpixel.bridge.Bridge;
import org.bukkit.event.Listener;

public class startGame implements Listener {
    Bridge plugin;
    public startGame(Bridge plugin) {
        this.plugin = plugin;
    }
}
